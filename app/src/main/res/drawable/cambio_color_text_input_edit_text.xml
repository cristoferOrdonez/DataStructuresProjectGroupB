<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true" android:color="@color/cian_titulo_evento_pagina_principal" />
    <!-- Default state -->
    <item android:color="@color/black" />
</selector>