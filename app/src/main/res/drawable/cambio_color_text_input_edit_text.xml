<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused state -->
    <item android:state_focused="true" android:color="@color/azul_botones" />
    <!-- Default state -->
    <item android:color="@color/white" />
</selector>